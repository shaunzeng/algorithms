import {
    selectionSort,
    bubbleSort,
    recursiveBubbleSort,
    insertionSort,
    mergeSort,
    heapSort
} from "./sorting_algorithm";
import * as logger from 'simple-node-logger';
import { MinIntHeap } from './data_structure';

const log = logger.createSimpleLogger();

log.log('hola');